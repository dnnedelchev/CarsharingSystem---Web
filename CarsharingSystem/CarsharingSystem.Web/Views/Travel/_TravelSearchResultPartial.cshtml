@model IList<CarsharingSystem.Web.ViewModels.Travel.SearchTravelReturnViewModel>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>От</th>
            <th>Отклонение</th>
            <th>До</th>
            <th>Отклонение</th>
            <th>Шофьор</th>
            <th>Дата</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <td>@(i + 1)</td>
                <td>@Model[i].AddressFrom</td>
                <td>@Model[i].DistanceFrom</td>
                <td>@Model[i].AddressTo</td>
                <td>@Model[i].DistanceTo</td>
                <td><a href=@("/User/Show/" + Model[i].DriverUsername)>@Model[i].DriverUsername</a></td>
                <td>@Model[i].TravelDate.ToShortDateString()</td>
            </tr>
        }
    </tbody>
</table>